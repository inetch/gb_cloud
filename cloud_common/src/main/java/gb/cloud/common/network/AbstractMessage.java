package gb.cloud.common.network;

/*Don't know, does not needed now, for further use
* */
public class AbstractMessage {
    private String text;
    public void setText(String text) {
        this.text = text;
    }
}
